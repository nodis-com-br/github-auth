name: CI
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - run: |
          curl -L -u "github-app:${GITHUB_TOKEN}" charts.nodis.com.br 2> /dev/null
          curl -H "Authorization: token ${GITHUB_TOKEN}" https://api.github.com/orgs/nodis-com-br 2> /dev/null | jq -r

      
